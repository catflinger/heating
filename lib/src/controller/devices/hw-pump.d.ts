import { Switchable } from "../switchable";
import { IControllerSettings } from "../types";
export declare class HWPump extends Switchable {
    private settings;
    constructor(settings: IControllerSettings);
}
